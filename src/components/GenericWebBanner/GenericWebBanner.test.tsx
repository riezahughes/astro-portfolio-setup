import React from "react";
import { render } from "@testing-library/react";
import GenericWebBanner from "./GenericWebBanner";

describe("Using module: GenericWebBanner", () => {
	it("Can see the icon appear", () => {
		expect(true).toBe(true);
	});
});
